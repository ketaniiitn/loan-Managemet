import React from "react";
import LoginRegister from "@/components/LoginRegister"; // ✅ adjust path as per your folder

const LoginPage = () => {
  return (
    <main>
      <LoginRegister />
    </main>
  );
};

export default LoginPage;